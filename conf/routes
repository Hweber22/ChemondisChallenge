PUT /freeTimeslots/:typ/:name          controllers.AppointmentController.setFreeTimeslots(typ: String, name: String)
GET /appointments                      controllers.AppointmentController.appointments(persons: List[String])