PUT /freeTimeslots/:name/:typ          controllers.AppointmentController.setFreeTimeslots(name: String, typ: String)
GET /appointments             controllers.AppointmentController.appointments(persons: List[String])